// Re-export DashboardShell as the standard wrapper for all dashboard-area pages.
// All pages that previously used <NavbarWrapper /> now get the full sidebar layout.
export { default } from "./DashboardShell";
